import { HeaderProps } from "../../../models/components/header/headerTypes";
import styled from "styled-components";

/**
 * Header wrapper with css styles
 */
export const StyledHeader = styled.header<HeaderProps>`
`;